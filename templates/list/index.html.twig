{% extends 'base.html.twig' %}

{% block title %}Hello ListController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
    <div class="example-wrapper">
        <h1>Привет </h1>
        <ul>
{#            {{ a.getEmail() }}#}
        </ul>
        <ul>
            {#            {% for n in num %}#}
            {#                <li>{{ n.id }}</li>#}
            {#                <li>{{ n.number }}</li>#}
            {#            {% endfor %}#}
        </ul>


        <table class="table">
            <thead>
            <tr>
                <th scope="col">id</th>
                <th scope="col">number</th>
            </tr>
            </thead>
            <tbody>
            {% for n in num %}
                <tr>
                    <th scope="row">{{ n.id }}</th>
                    <td>{{ n.number }}</td>
{#                    <td><a href="{{ path('remove_number', {'number': number.id}) }}" class="btn btn-primary"></a>Удалить</td>#}
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}

